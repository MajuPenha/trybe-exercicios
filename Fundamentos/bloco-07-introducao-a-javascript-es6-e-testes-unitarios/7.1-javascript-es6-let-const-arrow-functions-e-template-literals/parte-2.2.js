const longestWord = frase => {
  const arr = frase.split(' ')
  // console.log(frase.split(' ').split(''))
  for(let i = 0; i < arr.length; i++) {
    let numb = arr[i].split('').length
    
  }
}

longestWord('maria julia domenciano')